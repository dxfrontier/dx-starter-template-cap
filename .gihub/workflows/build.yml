name: Build Container Image

on:
  repository_dispatch:
    types: [custom_event]

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: echo "Event triggered"
