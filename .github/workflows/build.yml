name: Build Container Image and Push to ghcr.io

on:
  repository_dispatch:
    types: [container_build]

permissions:
  contents: read
  packages: write

jobs:
  build_and_deploy:
    name: 🚀 Build and Deploy
    runs-on: ubuntu-latest
    env:
      NODE_VERSION: 20

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Install Dependencies
        run: |
          echo "📦 Installing Node.js dependencies..."
          # Try npm ci first, fallback to npm install if lock file is out of sync
          if ! npm ci; then
            echo "⚠️ npm ci failed, falling back to npm install..."
            npm install
          fi
          echo "✅ Dependencies installed"

      - name: 🛠️ Install CAP Development Kit
        run: |
          echo "🛠️ Installing SAP CAP Development Kit globally..."
          npm install -g @sap/cds-dk
          echo "✅ CAP Development Kit installed"

      - name: 🏗️ Build CAP Application
        run: |
          echo "🏗️ Building CAP application..."
          export CDS_REQUIRES_AUTH_AMS_DCLGENERATIONPACKAGE="$IMAGE_NAME"
          echo "🔧 Set CDS_REQUIRES_AUTH_AMS_DCLGENERATIONPACKAGE=$IMAGE_NAME"
          npm run build
          echo "✅ CAP application built"
        env:
          IMAGE_NAME: ${{ github.event.client_payload.image || inputs.image_name }}

      - name: 🔐 Generate AMS DCL Files
        run: |
          echo "🔐 Generating AMS DCL files..."
          # The build script already includes AMS DCL generation
          echo "✅ AMS DCL files generated (included in build step)"

          # List generated files for debugging
          if [ -d "gen/policies" ]; then
            echo "📋 Generated AMS files:"
            find gen/policies -name "*.dcl" -type f | head -10
          else
            echo "⚠️ No gen/policies directory found"
          fi

      - name: 🔒 Registry Login
        run: |
          echo "🔐 Authenticating with container registry..."
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io \
            -u "dx.local" \
            --password-stdin
          echo "✅ Registry login successful"

      - name: 🐳 Build and Push Application Image
        run: |
          echo "🐳 Building Docker image for '$IMAGE_NAME' with tag 'latest' ..."
          docker build -t ghcr.io/dxfrontier/$IMAGE_NAME:latest ./
          echo "✅ Image built."

          echo "📤 Pushing image to GHCR..."
          docker push ghcr.io/dxfrontier/$IMAGE_NAME:latest
          echo "✅ Image pushed."
        env:
          IMAGE_NAME: ${{ github.event.client_payload.image || inputs.image_name }}

      - name: 🔐 Build and Push AMS Policies Deployer Image
        run: |
          echo "🔐 Building AMS Policies Deployer image..."

          # Create temporary directory for AMS deployer
          mkdir -p /tmp/ams-deployer

          # Create AMS deployer files
          echo "📝 Creating AMS deployer files..."

          # Create package.json for AMS deployer
          echo '{
            "name": "ams-dcl-content-deployer",
            "version": "1.0.0",
            "description": "AMS DCL Content Deployer",
            "main": "index.js",
            "dependencies": {
              "@sap/ams": "^3"
            },
            "scripts": {
              "start": "npx --package=@sap/ams deploy-dcl -d /app/dcl"
            }
          }' > /tmp/ams-deployer/package.json

          # Create Dockerfile for AMS deployer
          cat > /tmp/ams-deployer/Dockerfile << 'EOF'
          FROM node:20-alpine

          # Create a non-root user with specific UID/GID
          RUN addgroup -g 1001 -S nodejs && \
              adduser -S nodejs -u 1001 -G nodejs

          WORKDIR /app

          # Copy package.json and install dependencies
          COPY ./package.json /app/package.json
          RUN npm install --production

          # Copy DCL files
          COPY ./dcl /app/dcl

          # Change ownership of the app directory to the nodejs user
          RUN chown -R 1001:1001 /app

          # Switch to non-root user using numeric UID
          USER 1001

          CMD ["npm", "start"]
          EOF
          
          # Copy DCL files from the generated policies directory to the deployer build context
          if [ -d "./gen/policies/dcl" ]; then
            echo "📋 Copying generated DCL files to build context..."
            cp -r ./gen/policies/dcl /tmp/ams-deployer/dcl
          elif [ -d "./gen/policies" ]; then
            echo "📋 Copying generated policies directory to build context..."
            cp -r ./gen/policies /tmp/ams-deployer/dcl
          else
            echo "⚠️ No generated policies found, creating empty dcl directory"
            mkdir -p /tmp/ams-deployer/dcl
            echo "⚠️ Warning: AMS deployer will have no policies to deploy!"
          fi

          # List what we're copying for debugging
          echo "📋 Contents of AMS deployer build context:"
          ls -la /tmp/ams-deployer/
          if [ -d "/tmp/ams-deployer/dcl" ]; then
            echo "📋 DCL files to be deployed:"
            find /tmp/ams-deployer/dcl -name "*.dcl" -type f | head -10
          fi

          # Build the AMS deployer image
          echo "🐳 Building AMS deployer image..."
          docker build -t ghcr.io/dxfrontier/$IMAGE_NAME-ams-deployer:latest /tmp/ams-deployer/
          echo "✅ AMS deployer image built."

          # Push the AMS deployer image
          echo "📤 Pushing AMS deployer image to GHCR..."
          docker push ghcr.io/dxfrontier/$IMAGE_NAME-ams-deployer:latest
          echo "✅ AMS deployer image pushed."
        env:
          IMAGE_NAME: ${{ github.event.client_payload.image || inputs.image_name }}

      - name: 🏥 Health Check - Wait for Pod to Spawn
        if: ${{ (github.event.client_payload.app_url != '' && github.event.client_payload.app_url != null) || (inputs.app_url != '' && inputs.app_url != null) }}
        run: |
          APP_URL="${{ github.event.client_payload.app_url || inputs.app_url }}"
          MAX_HEALTH_CHECK_ATTEMPTS="${{ github.event.client_payload.max_health_check_attempts || inputs.max_health_check_attempts }}"
          HEALTH_CHECK_INTERVAL="${{ github.event.client_payload.health_check_interval || inputs.health_check_interval }}"

          # Set defaults if not provided
          MAX_HEALTH_CHECK_ATTEMPTS=${MAX_HEALTH_CHECK_ATTEMPTS:-60}
          HEALTH_CHECK_INTERVAL=${HEALTH_CHECK_INTERVAL:-10}

          if [ -z "$APP_URL" ]; then
            echo "⚠️ No app URL provided, skipping health check"
            exit 0
          fi

          # Construct health endpoint URL
          HEALTH_URL="${APP_URL}/health"

          echo "🔍 Starting health check for: $HEALTH_URL"
          echo "⏱️ Will attempt $MAX_HEALTH_CHECK_ATTEMPTS times with $HEALTH_CHECK_INTERVAL second intervals"

          ATTEMPT=0
          SUCCESS=false

          while [ $ATTEMPT -lt $MAX_HEALTH_CHECK_ATTEMPTS ]; do
            ATTEMPT=$((ATTEMPT + 1))
            echo "🔄 Attempt $ATTEMPT/$MAX_HEALTH_CHECK_ATTEMPTS..."

            # Call the health endpoint and capture both status code and response body
            RESPONSE=$(curl -s -w "\n%{http_code}" --connect-timeout 10 --max-time 30 "$HEALTH_URL" 2>/dev/null || echo -e "\n000")

            # Split response into body and status code
            HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
            BODY=$(echo "$RESPONSE" | sed '$d')

            echo "📊 HTTP Status: $HTTP_CODE"

            # Check if we got HTTP 200 and the response contains "status":"UP"
            if [ "$HTTP_CODE" = "200" ] && echo "$BODY" | grep -q '"status":"UP"'; then
              echo "✅ SUCCESS! Application is healthy"
              echo "🎉 Pod has successfully spawned and is responding correctly"
              SUCCESS=true
              break
            else
              if [ "$HTTP_CODE" = "000" ]; then
                echo "⏳ Cannot connect to health endpoint. Pod may not be ready yet..."
              elif [ "$HTTP_CODE" != "200" ]; then
                echo "⏳ Health endpoint returned HTTP $HTTP_CODE. Waiting for pod to be ready..."
              else
                echo "⏳ Health endpoint returned HTTP 200 but status is not UP."
              fi

              if [ $ATTEMPT -lt $MAX_HEALTH_CHECK_ATTEMPTS ]; then
                echo "   Waiting $HEALTH_CHECK_INTERVAL seconds before next attempt..."
                sleep $HEALTH_CHECK_INTERVAL
              fi
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "❌ FAILURE: Application did not become healthy within the timeout period"
            exit 1
          fi

      - name: 🎉 Deployment Complete
        run: |
          echo "✅ Container image built and pushed successfully"
          echo "✅ AMS Policies Deployer image built and pushed successfully"
          APP_URL="${{ github.event.client_payload.app_url || inputs.app_url }}"
          if [ -n "$APP_URL" ]; then
            echo "✅ Application health check passed"
            echo "🌐 Application URL: $APP_URL"
          fi
